// Code generated by ent, DO NOT EDIT.

package ent

import (
	"dgut-icourse/ent/object"
	"dgut-icourse/ent/other"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// Other is the model entity for the Other schema.
type Other struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the OtherQuery when eager-loading is set.
	Edges        OtherEdges `json:"edges"`
	object_other *uuid.UUID
	selectValues sql.SelectValues
}

// OtherEdges holds the relations/edges for other nodes in the graph.
type OtherEdges struct {
	// Object holds the value of the object edge.
	Object *Object `json:"object,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ObjectOrErr returns the Object value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OtherEdges) ObjectOrErr() (*Object, error) {
	if e.Object != nil {
		return e.Object, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: object.Label}
	}
	return nil, &NotLoadedError{edge: "object"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Other) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case other.FieldID:
			values[i] = new(sql.NullInt64)
		case other.ForeignKeys[0]: // object_other
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Other fields.
func (o *Other) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case other.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			o.ID = int(value.Int64)
		case other.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field object_other", values[i])
			} else if value.Valid {
				o.object_other = new(uuid.UUID)
				*o.object_other = *value.S.(*uuid.UUID)
			}
		default:
			o.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Other.
// This includes values selected through modifiers, order, etc.
func (o *Other) Value(name string) (ent.Value, error) {
	return o.selectValues.Get(name)
}

// QueryObject queries the "object" edge of the Other entity.
func (o *Other) QueryObject() *ObjectQuery {
	return NewOtherClient(o.config).QueryObject(o)
}

// Update returns a builder for updating this Other.
// Note that you need to call Other.Unwrap() before calling this method if this Other
// was returned from a transaction, and the transaction was committed or rolled back.
func (o *Other) Update() *OtherUpdateOne {
	return NewOtherClient(o.config).UpdateOne(o)
}

// Unwrap unwraps the Other entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (o *Other) Unwrap() *Other {
	_tx, ok := o.config.driver.(*txDriver)
	if !ok {
		panic("ent: Other is not a transactional entity")
	}
	o.config.driver = _tx.drv
	return o
}

// String implements the fmt.Stringer.
func (o *Other) String() string {
	var builder strings.Builder
	builder.WriteString("Other(")
	builder.WriteString(fmt.Sprintf("id=%v", o.ID))
	builder.WriteByte(')')
	return builder.String()
}

// Others is a parsable slice of Other.
type Others []*Other
